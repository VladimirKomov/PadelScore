{
  "name": "padelscore",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --experimental-strip-types --loader ./tests/arkts-loader.mjs --test tests/*.test.ts"
  }
}
